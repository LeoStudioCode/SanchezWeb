import 'package:flutter/material.dart';

class BibliotecaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Biblioteca de Artículos'),
      ),
      body: Center(
        child: Text('Pantalla de Biblioteca de Artículos'),
      ),
    );
  }
}
